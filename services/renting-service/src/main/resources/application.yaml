server:
  port: 8083

spring:
  application:
    name: renting-service
  datasource:
    url: jdbc:postgresql://localhost:5435/rental_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false

app:
  internal-secret: fu-car-internal-secret-change-me

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
